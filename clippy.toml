too-many-arguments-threshold = 15
type-complexity-threshold = 500
disallowed-types = [
    { path = "std::collections::HashMap", reason = "use DHashMap and dmap::new() to prevent non-determinism" },
]
