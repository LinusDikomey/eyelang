[package]
name = "eyelang"
version = "0.1.0"
edition = "2021"

[features]
default-features = ["llvm-backend"]
llvm-backend = ["inkwell"]

[dependencies]
inkwell = { git = "https://github.com/TheDan64/inkwell", branch = "master", features = ["llvm12-0"], optional = true }