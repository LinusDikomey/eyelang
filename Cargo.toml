[package]
name = "eyelang"
version = "0.1.0"
edition = "2021"

[features]
# default = [ "llvm-backend" ]
llvm-backend = ["llvm-sys"]

[dependencies]
colored = "2.0"
llvm-sys = { version = "130.0.3", optional = true }